# 20260715-2010-release-closure

中文结论：当前提交是纯活动 SpinalHDL 候选，58/81 功能诊断通过，但严格告警、随机、性能、系统、Vivado/FPGA 和外部审核尚未闭环，因此只能提交 Draft PR，不能宣称完全重构完成。

- Status: draft; functional diagnostics pass, required release gates do not
- Branch / Base SHA / Head SHA: `refactor/20260715-2010-release-closure` / `56d74f4b92d85e6403689cbbe4f7b4b460e8f63a` / reviewed implementation `9a29409242bf29436bf553e584b87018fb3f6fa6`
- Owner / Agent: Codex `/root`
- Selected boundary and selection reason: release-closure audit of the committed pure-Spinal `core_top`; this is the narrowest boundary that can determine which repository completion gates are real, missing, or failing before any further implementation.
- Golden reference and locked tool versions: `a158aa8ab4d49cece1a0fe488d7ac7dc02bd8cf6`; versions are locked by `reference/manifest.lock`.
- Behavior contract: `AGENTS.md` completion definition, `docs/contracts/candidate-closure.md`, and `docs/contracts/core-top-compat.md`.
- Files changed: publish the reproducible package at `rtl/mycpu_top.v`; remove the obsolete `CPUCoreFlat` RTL and 15 inactive draft Scala sources; bind the publication specs and exact lint waiver to the canonical path; update affected tests and documentation.
- Attempts and failures: Windows `scala-check` incorrectly mapped `/opt` to `D:/opt`; the locked WSL run passed. A focused Python command used package-style test names although `tests/` is not a package; direct discovery passed. Two WSL automation attempts inherited linked-worktree `GIT_DIR`; the accepted clean-clone run explicitly removed `GIT_DIR/GIT_WORK_TREE` and passed 390/390 with zero skip. The first func58 build omitted `CHIPLAB_HOME` and failed before compilation; the suite was reconfigured and rerun from the locked environment. Claude review job `8c1ce5c9c1544d80935d48d6fddbb9a8` failed because `GEEKPIE_CLAUDE_API_KEY` is absent; the fallback is explicitly local and cannot authorize readiness.
- Commands and gate results: doctor PASS; locked Scala 4/4 PASS; reproducible generation 2/2 PASS; official 49-port contract, publication consistency, port, Yosys, candidate closure, typed AXI, and reachability PASS. The 73-warning aggregate manifest produces a diagnostic zero-residual closure, but it is not a policy-compliant set of per-warning `lint-waivers.yml` records, so required lint is FAIL. Clean Linux automation is 390/390 with zero skip. Chiplab doctor passes at commit `9a29409`. func58 is 58/58 functional PASS and func81 reaches score 81 with live DiffTest and no mismatch; both strict gates FAIL on 40 DUT plus 365 official-environment compile warnings.
- Functional/performance/resource delta: functional diagnostics are 58/58 and 81/81. Random, perf20, U-Boot, Linux, Fmax, LUT/FF/BRAM, timing, bitstream, and hardware were not measured.
- Residual risks: the source is not strict-zero clean and the aggregate waiver does not meet repository policy. Six warning-backed debts remain: fixed-only `TLBNUM`, unused BTB delete, unused PRELD hint, ignored AXI `rresp/bresp`, and write-only BRK state. Random is blocked by an empty locked vector set. The active official 32-entry predictor conflicts with the completion contract's historical 64-entry target. The canonical package is LACC-off and lacks a release-equivalent LACC-on matrix. Performance, system, Vivado/FPGA, and external claim review remain open.
- Rollback: revert this iteration's commits; the consolidated branch and original dirty worktree remain untouched.
- PR URL or awaiting state: awaiting evidence commit, push, and Draft PR; never ready or merged automatically.
- Next unblocked candidates: make lint waivers policy-compliant; add fail-closed func58/func81 wrappers; restore locked random vectors; run perf20/system/Vivado; reconcile BTB authority and LACC profiles.
