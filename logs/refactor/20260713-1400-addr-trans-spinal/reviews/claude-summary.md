# Claude 审核状态

状态：`unavailable`。

`mcp__claude_review__review` 在模型启动前返回 `GEEKPIE_CLAUDE_API_KEY` 缺失。没有把本地判断冒充 Claude 结论。当前只接受窄范围 claim：生成可复现和 TLB 2-state cycle diff 通过；addr_trans 独立差分、整机功能、随机 DiffTest、性能、Linux、Vivado 均保持 open。
