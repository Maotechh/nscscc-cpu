# 20260715 official source provenance audit

## Goal and inputs

Bind the default refactor profile to the official nested OpenLA500 source and
separate historical team additions. This is a provenance result, not behavioral
equivalence or whole-core acceptance.

- ECHO base: `b7b6993352e8c05b8d0b39b169134ba119a49833`
- official repository: `../chiplab/IP/myCPU`
- official revision: `aa3bde1f3e720e71c2c78d6b81930d797b810149`
- historical diagnostic revision: `a158aa8ab4d49cece1a0fe488d7ac7dc02bd8cf6`

## Commands and exits

| command | exit | result |
| --- | --- | --- |
| `git -C ../chiplab/IP/myCPU ls-tree -r aa3bde1...` | 0 | 29 official files |
| `git ls-tree -r a158aa8... -- rtl` | 0 | 25 historical RTL files |
| `python3 -I tools/official_source_audit.py ...` | 0 | JSON and Markdown generated |
| repeated audit generation and `diff` of SHA lists | 0 | JSON and Markdown hashes stable |
| `python3 -I tests/test_official_source_audit.py -v` | 0 | 3 focused classification tests |
| `make test-local LOCAL_TMP_ROOT=/tmp` | 0 | 369 tests, 8.437 s, `OK` |

The first generator run failed on an empty Git pathspec. The second exposed quoted
non-ASCII paths from `git ls-tree`. The final implementation uses `ls-tree -z`
and NUL splitting, so official Chinese documentation paths are recorded exactly.

## Outputs and hashes

- `reference/source-audit/aa3bde1-vs-a158aa8.json`:
  `15ed0df665e775a8f9da84ac60f7fcfc1c75a52cfd7ac83636e81125f26a1f52`
- `reference/source-audit/aa3bde1-vs-a158aa8.md`:
  `da3e894e578650cb57e518f998d899babb71f0faee9b8806c94c91d05ef4a164`

The official tree contains 21 behavioral Verilog/header files, two memory IP
archives, and documentation/license material. The a158 RTL comparison contains
20 basename overlaps, three candidate-only enhancements (`lacc_core.v`,
`lacc_demo.v`, `store_buffer.v`), and two dead/backup candidates (`btb.v.bak`,
`regfile_dual.v`). Hashes show that several basename overlaps differ from aa3;
a basename match is not treated as proof of official identity.

## Claim and residual risk

Qualified provenance: aa3bde1 is the official default source contract; a158aa8
is diagnostic only. LACC/store buffer remain opt-in and backups remain inactive.
This audit does not establish behavioral equivalence of current Spinal RTL,
Cache/TLB/AXI correctness, complete functional tests, or release readiness.

Rollback: revert the source-audit ECHO commit. Do not modify or clean the nested
official checkout or chiplab working tree.
